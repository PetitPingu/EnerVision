# Épinglé exactement (pas une plage) : client et serveur sont deux images
# Docker buildées séparément à des moments différents - une plage ouverte
# laisserait le client (apps/prediction/requirements.txt) dériver au-dessus
# du serveur au prochain rebuild, ce qui casse les appels REST (404).
mlflow==2.22.5
psycopg2-binary>=2.9
boto3>=1.34
