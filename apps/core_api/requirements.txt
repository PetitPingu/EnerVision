# Chemin relatif résolu depuis le dossier courant au moment du `pip install`
# (cd apps/core_api && pip install -r requirements.txt) : un futur Dockerfile
# doit préserver la même profondeur relative apps/core_api <-> packages/.
-e ../../packages/mockapi-client
-e ../../packages/db-schema
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31
bcrypt>=4.1
PyJWT>=2.8
redis>=5.0.1
