# Chemin relatif résolu depuis le dossier courant au moment du `pip install`
# (cd apps/etl-worker && pip install -r requirements.txt) : un futur Dockerfile
# doit préserver la même profondeur relative apps/etl-worker <-> packages/.
-e ../../packages/mockapi-client
python-dotenv>=1.0
