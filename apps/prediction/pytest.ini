[pytest]
markers =
    integration: tape un vrai Postgres/MinIO (docker compose) au lieu de mocks - voir tests/integration/conftest.py
; Exclu par defaut : `python -m pytest tests/ -q` (voir CI) reste la suite
; unitaire rapide. Lancer les tests d'integration avec `-m integration`.
addopts = -m "not integration"
