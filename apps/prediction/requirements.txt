# Chemin relatif résolu depuis le dossier courant au moment du `pip install`
# (cd apps/prediction && pip install -r requirements.txt) : un futur Dockerfile
# doit préserver la même profondeur relative apps/prediction <-> packages/.
-e ../../packages/db-schema
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.2
scikit-learn>=1.4
minio>=7.2
# Épinglé exactement comme mlflow/requirements.txt (serveur), pas une plage -
# deux images buildées séparément, une plage ouverte laisserait le client
# dériver au-dessus du serveur au prochain rebuild (404 sur les endpoints REST).
mlflow==3.16.1
boto3>=1.34
